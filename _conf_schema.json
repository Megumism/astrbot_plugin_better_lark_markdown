{
    "card_send_mode": {
        "description": "飞书消息发送方式",
        "type": "string",
        "default": "auto",
        "options": ["auto", "reply", "direct"],
        "hint": "auto: 仅当回复内容不同于上一次时采用回复形式发送；reply: 所有消息回复发送，即astrbot默认行为。direct: 所有消息直接发送。"
    }
}
